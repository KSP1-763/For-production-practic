<Window x:Class="FOR_PRODUCTION_PRACTICE.Windows.UserWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FOR_PRODUCTION_PRACTICE.Windows"
        mc:Ignorable="d"
        Title="UserWindowV" Height="450" Width="800">
    <Grid Background="#F0F2F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#2ECC71" Padding="10">
            <Grid>
                <TextBlock x:Name="TitleText" Text="Сотрудник" Foreground="White" FontSize="18" FontWeight="Bold"/>
                <Button Content="Выйти" HorizontalAlignment="Right" Background="#E74C3C" Foreground="White" BorderThickness="0" Padding="10,5" Click="LogoutButton_Click"/>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Button Grid.Row="0" Content="➕ Новая заявка" Background="#27AE60" Foreground="White" BorderThickness="0" Padding="10,5" HorizontalAlignment="Left" Click="NewRequest_Click"/>

            <DataGrid x:Name="RequestsGrid" Grid.Row="1" AutoGenerateColumns="False" Margin="0,10,0,0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="№" Binding="{Binding Id_request}" Width="50"/>
                    <DataGridTextColumn Header="Номер" Binding="{Binding RequestNumber}" Width="120"/>
                    <DataGridTextColumn Header="Дата" Binding="{Binding DateReceived, StringFormat=dd.MM.yyyy}" Width="100"/>
                    <DataGridTextColumn Header="Оборудование" Binding="{Binding EquipmentType}" Width="120"/>
                    <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="150"/>
                    <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="100"/>
                    <DataGridTextColumn Header="Инженер" Binding="{Binding EngineerName}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</Window>
